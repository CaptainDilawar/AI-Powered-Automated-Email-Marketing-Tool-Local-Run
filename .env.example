
# -----------------------------------------------------------------------------
# CORE API KEYS & SETTINGS
# -----------------------------------------------------------------------------

# -- Groq AI API Key --
# Used for generating personalized email content with LLaMA3.
# Get yours from: https://console.groq.com/keys
GROQ_API_KEY="your_groq_api_key"

# -- Capsolver API Key --
# Used for automatically solving Google's reCAPTCHA during lead scraping.
# Get yours from: https://capsolver.com/
CAPSOLVER_API_KEY="your_capsolver_api_key"

# -- Database Encryption Key --
# A strong, secret password used to encrypt sensitive data in the database (e.g., user names, emails, API keys).
# IMPORTANT: Use a secure, randomly generated string (e.g., from a password manager).
DB_ENCRYPTION_KEY="your_strong_and_secret_encryption_key"

# -----------------------------------------------------------------------------
# EMAIL SENDING (SMTP) & REPLY ANALYSIS (IMAP)
# -----------------------------------------------------------------------------

# -- SMTP Server Settings --
# Used for sending emails. The example below is for Brevo (formerly Sendinblue).
# Replace with your email provider's SMTP details.
SMTP_SERVER="smtp-relay.brevo.com"
SMTP_PORT=587
SMTP_USERNAME="your_smtp_login_email@example.com"
SMTP_PASSWORD="your_smtp_password_or_api_key" # For Brevo, this is the SMTP Key.

# -- IMAP Server Settings --
# Used for fetching and analyzing replies to your campaigns.
# IMPORTANT: For Gmail, you will need to generate an "App Password".
# How-to: https://support.google.com/accounts/answer/185833
IMAP_SERVER="imap.gmail.com"
EMAIL="your_monitoring_email@gmail.com" # The email account to check for replies.
EMAIL_PASSWORD="your_google_app_password" # The App Password you generated.

# -- Reply-To Email --
# The email address where you want to receive replies.
REPLY_TO_EMAIL="your_reply_to_email@example.com"

# -----------------------------------------------------------------------------
# APPLICATION SETTINGS
# -----------------------------------------------------------------------------

# -- API Base URL --
# The URL where the backend FastAPI server is running.
# This should not be changed unless you modify the default ports.
API_BASE_URL="http://localhost:8000"
